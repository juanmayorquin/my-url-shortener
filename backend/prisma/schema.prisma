datasource db {
  provider = "postgresql"
}

generator client {
  provider = "prisma-client-js"
}

model ShortUrl {
  id        Int      @id @default(autoincrement())
  code      String   @unique
  targetUrl String
  createdAt DateTime @default(now())
}
